# CLAUDE.md (Session 1 — overloaded)

tasklight is a personal CLI for task lists. It uses SQLite — see docs/architecture.md.

Always run tests before commit.

We're planning an export command next. Export should write JSON to stdout.

Why SQLite: single file, no server, good for a solo side project.

When editing, follow the layout in docs/architecture.md.
